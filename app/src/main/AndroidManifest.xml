<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sweaty">

    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sweaty">

        <!--
               Set to true if your app is Standalone, that is, it does not require the handheld
               app to run.
        -->
        <activity android:name=".Intro">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".ToPlateActivity" />
        <activity android:name=".ToWarmupActivity" />
        <activity android:name=".ToTemplateActivity" />
        <activity android:name=".ToBmiActivity" />
        <activity android:name=".ToTimerActivity" />

        <activity android:name=".activities.BMIActivity" />
        <activity android:name=".activities.BMISecondActivity" />
        <activity android:name=".activities.BMIThirdActivity" />

        <activity android:name=".activities.MacroFirstActivity" />
        <activity android:name=".activities.MacrosThirdActivity" />
        <activity android:name=".activities.MacrosSecondActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>